<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Custom permissions for privileged properties -->
    <permission
        android:name="com.automotive.carsettings.permission.PRIVILEGED"
        android:protectionLevel="signature" />

    <permission
        android:name="com.automotive.carsettings.permission.SYSTEM"
        android:protectionLevel="signature|privileged" />

    <application
        android:name=".CarSettingsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">

        <service
            android:name=".CarSettingsService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="systemExempted">
            <intent-filter>
                <action android:name="com.automotive.carsettings.service.BIND_SERVICE" />
            </intent-filter>
        </service>

    </application>

</manifest>
